# Core
python-dotenv>=1.0.0

# Image/Video Generation (fal.ai)
fal-client>=0.4.0

# Voice Generation (ElevenLabs)
elevenlabs>=1.0.0

# Audio Processing
pydub>=0.25.1

# HTTP requests (thumbnail image fetching, BGM download)
requests>=2.31.0

# Image Processing (thumbnail text overlays)
Pillow>=10.0.0

# YouTube Upload
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# HTTP & Async
httpx>=0.27.0
aiofiles>=23.2.0

# AMV Download
yt-dlp>=2024.1.0

# Claude Vision (used by analyze_amv.py for scene descriptions)
anthropic>=0.25.0

# Optional: External LLM (only needed in standalone mode, not when Claude orchestrates)
# openai>=1.30.0
